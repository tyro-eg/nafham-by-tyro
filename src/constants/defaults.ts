/**
 * Default Constants
 *
 * This file contains default values used throughout the application.
 * These are temporary defaults that should be replaced with dynamic values
 * from the API once the backend implementation is complete.
 */

/**
 * Default subject ID for sessions
 * TODO: Replace with dynamic subject selection once backend supports it
 */
export const DEFAULT_SUBJECT_ID = 15;

/**
 * Default package ID for private sessions
 * TODO: Replace with dynamic package selection from instructor packages
 */
export const DEFAULT_PACKAGE_ID = 1;
